export interface Herb {
  id: number;
  name: string;
  category: string;
  primary_uses: string;
  contraindications: string;
  effects_actions: string;
  storage: string;
  application_methods: string;
  pairs_well_with: string;
}
